---
- name: test
  hosts: all
  gather_facts: false

  tasks:
    - name: Basic usage
      ansible.builtin.debug:
        msg:
          - "'{{ lookup('ansible.builtin.env', 'test') }}' is the test environment variable."
          - "'{{ test }}' is the test extra variable."
          - "'{{ username }}' is the username variable."
